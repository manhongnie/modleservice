# 历史配置

`models.real.json` 仅保存已移除的 Qwen3.5-0.8B、Whisper Tiny、中文 VITS 配置，用于阅读历史报告和兼容测试。它们已从当前 `var/models.sqlite3` 移除，不属于当前部署；不应按旧文档重新注册。

当前配置在上级的 `models.genai.json`、`models.retrieval-migrated.json`、`models.sherpa.json`、`models.media.json`。
